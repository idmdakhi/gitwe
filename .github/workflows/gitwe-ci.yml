name: GitWe Workflow

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  gitwe:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # مهم برای graph/status

      - name: Validate workflow config
        uses: ./.github/actions/gitwe
        with:
          command: validate
          config: .gitwe/gitwe.json

      - name: Doctor check
        uses: ./.github/actions/gitwe
        with:
          command: doctor

      - name: Start feature branch (PR context)
        if: github.event_name == 'pull_request'
        uses: ./.github/actions/gitwe
        with:
          command: start
          type: feature
          short-name: ${{ github.head_ref }}

      - name: Finish release (manual or on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: ./.github/actions/gitwe
        with:
          command: finish
          branch-name: ${{ github.ref_name }}
          push: true
